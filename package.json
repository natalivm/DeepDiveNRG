{
  "name": "deepdivenrg",
  "version": "1.0.0",
  "description": "DeepDiveNRG - Energy solutions website",
  "scripts": {
    "dev": "npx serve .",
    "build": "node scripts/build.js",
    "preview": "npx serve _site",
    "lint": "eslint app.js config.js sw.js",
    "format": "prettier --write .",
    "ci": "npm run lint && npm run build"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
